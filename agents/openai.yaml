interface:
  display_name: "年糕风格罗盘"
  short_description: "先看风格总览，再为新项目或旧项目改版推荐 3 种 UI 风格，解释取舍并生成可交接草稿"
  default_prompt: "Use $style-compass to help the user browse the local Style Gallery first when they are still exploring directions, then recommend three suitable UI styles for a new project or UI redesign, explain why each direction fits or conflicts with the project, and draft a handoff-ready DESIGN.md or UI-REFACTOR.md when needed."
